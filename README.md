# Certified Kubernetes Administrator Lab

Vagrant environment for the CKA lab.

# Requisites

- vagrant
- VirtualBox

If you are on macOS with `brew` you can run:

```shell
brew install virtualbox vagrant
```